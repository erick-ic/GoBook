# 1. 指定基础镜像
# 使用 Ubuntu 作为基础运行环境。这是一个标准的 Linux 发行版镜像，包含了完整的操作系统工具链。
# 注意：这会使得最终镜像体积较大（约 70MB+），适合需要完整系统工具或调试的场景。
FROM ubuntu

# 2. 复制编译好的二进制文件到镜像中
# 将宿主机当前目录下的 "gobook" 可执行文件，复制到镜像根目录下的 "/app" 目录。
# 注意：这里使用了绝对路径 "/app"，如果 "/app" 目录不存在，Docker 会自动创建。
COPY gobook /app/gobook

# 3. 设置工作目录
# 将容器内的当前工作目录切换为 "/app"。
# 后续的 RUN、CMD、ENTRYPOINT 等指令都会在这个目录下执行。
# 这有助于简化路径引用（例如后续如果要用到相对路径）。
WORKDIR /app

# 4. 指定容器启动时执行的命令
# 使用 ENTRYPOINT 定义容器启动时的默认执行程序。
# 当容器启动时，会直接运行 "/app/gobook" 这个二进制文件。
# 使用 JSON 数组格式（Exec 形式）可以确保信号正确传递，且不经过 shell 解析。
ENTRYPOINT ["/app/gobook"]